内嵌标签学习iframe
1.src:资源路径，可以是本地(相对路径)，也可以是网络资源(URL)
【注意：默认当前页面打开及加载src指向的资源】
2.width:设置显示区域的宽度
3.height:设置显示区域的高度
4.name:设置内嵌区域的名字，结合超链接标签的target属性使用。
5.作用：在当前网页中加载其他网页的资源，达到不同网页之间不相互干扰，并能在同一个网页中展现给用户的目的。
